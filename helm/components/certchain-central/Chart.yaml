apiVersion: v2
name: certchain-central
description: CertChain — shared infrastructure (Fabric CA, orderer, Keycloak broker, verification portal, Grafana)
type: application
version: 0.1.0
appVersion: "1.0.0"
